--[[
	Wave HUD entry: builds UI (WaveHudView) and runs logic (WaveHudController).
	Controller runs on require: WaveState remote, attributes, HP/money/enemies, game over, map vote, return to lobby.
]]
require(script.Parent.WaveHudController)
